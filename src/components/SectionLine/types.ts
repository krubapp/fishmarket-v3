/**
 * Section line (Figma: Section-lines, node 410:9145).
 * Horizontal section divider: 8px height, slate-100.
 */

export interface SectionLineProps {
  /** Applied to the root element. */
  className?: string;
}
