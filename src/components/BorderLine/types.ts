/**
 * Border line (Figma: border-line, node 470:1703).
 * Horizontal divider: 2px height, slate-200.
 */

export interface BorderLineProps {
  /** Applied to the root element. */
  className?: string;
}
